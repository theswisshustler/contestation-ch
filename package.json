{
  "name": "contestation-ch",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "contestation.ch — génération de requêtes en contestation de loyer initial (VD + GE)",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
